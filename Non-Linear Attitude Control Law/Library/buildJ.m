function [J] = buildJ(Jc, Is, It)
% Description: this function is an auxiliary function that removes the
% building of the J matrix from the main program.

% Initialize J Matrix
J = zeros(3, 3);

% Import Data from Input
Jc = reshape(Jc, 3, 3);





end